@import './styles/theme.wxss';

/* 全局 CSS 变量与重置 (无主题时的兜底值) */
page {
  --bg: #1a1412;
  --surface: #261d19;
  --card: #302621;
  --accent1: #cda869;
  --accent2: #658266;
  --accent3: #b85440;
  --accent4: #6b5c7d;
  --text: #e8ded1;
  --text-sub: #9e8e7c;
  --border: rgba(212, 175, 111, 0.15);
  --green: #71985a;
  --radius: 16px;

  background-color: var(--bg);
  color: var(--text);
  font-family: -apple-system, 'PingFang SC', 'Hiragino Sans GB', sans-serif;
  box-sizing: border-box;
}

view, text, scroll-view, image {
  box-sizing: border-box;
}

/* 隐藏滚动条 */
::-webkit-scrollbar {
  display: none;
  width: 0;
  height: 0;
  color: transparent;
}
